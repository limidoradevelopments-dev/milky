
import React from "react";

export default function VehiclesBasePage() {
  // This page can be used to redirect to a default sub-page
  // or display an overview if there are no sub-pages / if direct access is intended.
  // For now, it's a placeholder like other base route pages.
  return (
    <>
      {null}
    </>
  ); 
}
